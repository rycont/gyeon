export const submitButton = document.getElementById(
    'submit_button'
) as HTMLButtonElement

export const trialInput = document.getElementById(
    'answer_input'
) as HTMLInputElement

export const inputForm = document.getElementById(
    'answer_form'
) as HTMLFormElement

export const hintButton = document.getElementById(
    'hint_button'
) as HTMLButtonElement

// Add the missing elements export
export const elements = {
    body: document.body,
    // You can add other frequently accessed elements here if needed
};
